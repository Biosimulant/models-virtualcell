[pytest]
# Templates are starter scaffolds; their tests intentionally reference placeholder
# modules and should not run as part of the curated repo test suite.
norecursedirs = templates

# Multiple spaces contain `tests/test_space.py`. Importlib mode avoids module name
# collisions during collection.
addopts = --import-mode=importlib
