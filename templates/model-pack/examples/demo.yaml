# Demo config for my_pack modules

modules:
  counter:
    class: my_pack.Counter
    args:
      name: demo_counter
    min_dt: 0.1

  accumulator:
    class: my_pack.Accumulator
    args:
      initial: 0.0
    min_dt: 0.1

wiring:
  - from: counter.count
    to: [accumulator.value]
